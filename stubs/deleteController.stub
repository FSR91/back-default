<?php

namespace App\Http\Controllers;

use App\Commands\{{ modelName }}\DeleteCommand;
use App\Handlers\{{ modelName }}\DeleteHandler;

class Delete{{ modelName }}Controller extends Controller
{
    protected $handler;

    public function __construct (DeleteHandler $handler)
    {
        $this->handler = $handler;
    }

    public function __invoke(int $id)
    {
        $command = new DeleteCommand($id);

        return $this->handler->handle($command);
    }
}